<configuration>
    <if condition='property("debug").equals("true")'>
        <then>
            <include resource="logback-debug.xml"/>
        </then>
        <else>
            <include resource="logback-release.xml"/>
        </else>
    </if>
</configuration>